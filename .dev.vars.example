# Copy this file to .dev.vars for local wrangler dev overrides.
# .dev.vars is gitignored — never commit it.
# Add real secrets here (API keys, tokens, etc.) when needed.
#
# Example:
# SOME_API_KEY=your_key_here
