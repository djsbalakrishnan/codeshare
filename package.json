{
  "name": "codeshare",
  "version": "1.0.0",
  "private": true,
  "description": "DSA CodeShare — paste + share code snippets for Scaler classes",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
